@model RoomMates.Models.DBModel.ShopBill
@{
    ViewData["Title"] = "Add Shop Bill";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8 col-sm-10">
            <div class="card shadow-lg border-0 rounded-4 w-100" style="max-width: 700px;">
                <div class="card-header bg-primary text-white text-center py-3 rounded-top-4">
                    <h4 class="mb-0 fw-bold">Add Shop Bill</h4>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Save" method="post" novalidate>
                        <div class="mb-3">
                            <label asp-for="Date" class="form-label fw-semibold"></label>
                            <input asp-for="Date" class="form-control" type="date" />
                            <span asp-validation-for="Date" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="RoomRent" class="form-label fw-semibold"></label>
                            <input asp-for="RoomRent" class="form-control" type="number" step="0.01" placeholder="Enter Room Rent" />
                            <span asp-validation-for="RoomRent" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="EBBill" class="form-label fw-semibold"></label>
                            <input asp-for="EBBill" class="form-control" type="number" step="0.01" placeholder="Enter EB Bill" />
                            <span asp-validation-for="EBBill" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="WaterBill" class="form-label fw-semibold"></label>
                            <input asp-for="WaterBill" class="form-control" type="number" step="0.01" placeholder="Enter Water Bill" />
                            <span asp-validation-for="WaterBill" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="AkkaBill" class="form-label fw-semibold"></label>
                            <input asp-for="AkkaBill" class="form-control" type="number" step="0.01" placeholder="Enter Akka Bill" />
                            <span asp-validation-for="AkkaBill" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="WifiNet" class="form-label fw-semibold"></label>
                            <input asp-for="WifiNet" class="form-control" type="number" step="0.01" placeholder="Enter WiFi Bill" />
                            <span asp-validation-for="WifiNet" class="text-danger"></span>
                        </div>

                        <div class="mt-4 text-center d-flex justify-content-center gap-3 flex-wrap">
                            <button type="submit" class="btn btn-success px-5 shadow-sm">
                                <i class="bi bi-check-circle me-2"></i>Save
                            </button>

                            <button type="button" class="btn btn-danger px-5 shadow-sm" onclick="window.history.back();">
                                <i class="bi bi-x-circle me-2"></i>Close
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- ✅ Success Modal -->
<div class="modal fade" id="successModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content rounded-4 shadow position-relative">
            <button type="button" class="btn-close position-absolute top-0 end-0 m-3" data-bs-dismiss="modal" aria-label="Close"></button>
            <div class="modal-body text-center p-5">
                <div class="mb-3 text-success">
                    <i class="bi bi-check-circle-fill" style="font-size: 3rem;"></i>
                </div>
                <h4 class="fw-bold">Success!</h4>
                <p class="text-muted mb-0">@TempData["SuccessMessage"]</p>
                <div class="mt-4">
                    <button class="btn btn-primary px-4" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    @* ✅ Bootstrap Modal Show Script *@
    @if (TempData["SuccessMessage"] != null)
    {
        <script>
            document.addEventListener("DOMContentLoaded", function () {
                var modal = new bootstrap.Modal(document.getElementById('successModal'));
                modal.show();
            });
        </script>
    }
}
